language: cpp

sudo: required

branches:
  only:
  - master

os: linux
compiler: gcc
dist: bionic 

jobs:
  include:
  - stage: setup_env
    script: 
      - cd $TRAVIS_BUILD_DIR
      - cd Tools/setup
      - ./ubuntu.sh
      - cd $TRAVIS_BUILD_DIR
      - cmake --configure .
      - cmake --build .
      - cd build
      - make all
    
  
script:
  -   echo "hello from travis "